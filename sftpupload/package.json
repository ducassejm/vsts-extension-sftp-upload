{
  "name": "sftpupload",
  "version": "1.0.0",
  "description": "Upload files using SFTP",
  "main": "sftpupload/index.js",
  "scripts": {
    "test": "mocha sftpupload/tests/_suite.js",
    "create": "tfx extension create --manifest-globs vss-extension.json --rev-version",
    "build": "tsc --project sftpupload"
  },
  "keywords": [
    "SFTP",
    "SSH",
    "secure",
    "FTP"
  ],
  "author": "Jean-Marc Ducasse",
  "license": "ISC",
  "dependencies": {
    "scp2": "^0.5.0",
    "ssh2": "^0.5.5",
    "vsts-task-lib": "^2.0.5"
  },
  "devDependencies": {
    "@types/mocha": "^2.2.41",
    "@types/ssh2": "^0.5.35",
    "mocha": "^3.4.2"
  }
}
